{% assign upsell_product_handle = 'sample-product' %}
{% assign upsell_product = all_products[upsell_product_handle] %}

{% if upsell_product %}
<div style="border:1px solid #eee;border-radius:8px;padding:20px;margin:20px 0;
     text-align:center;font-family:'Arial',sans-serif;box-shadow:0 4px 10px rgba(0,0,0,0.05);
     transition:transform 0.2s ease;">
  <h3 style="margin-bottom:15px;color:#333;">✨ Customers also bought:</h3>
  <a href="{{ upsell_product.url }}" style="text-decoration:none;color:#333;display:block;">
    <img src="{{ upsell_product.featured_image | img_url: 'medium' }}" 
         alt="{{ upsell_product.title }}" 
         style="max-width:180px;margin-bottom:15px;border-radius:6px;" />
    <p style="font-weight:bold;font-size:16px;margin:5px 0;">{{ upsell_product.title }}</p>
    <p style="color:#27ae60;font-size:15px;margin:5px 0;">{{ upsell_product.price | money }}</p>
    <button style="background:#27ae60;color:#fff;padding:12px 24px;border:none;
            border-radius:25px;cursor:pointer;font-weight:bold;
            box-shadow:0 2px 6px rgba(0,0,0,0.2);">
      🛒 Add to Cart
    </button>
  </a>
</div>
{% endif %}
